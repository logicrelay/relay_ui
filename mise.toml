[tools]
ruby = "3.4.2"

[tasks.watch]
alias = "w"
run = "npm run watch &"

[tasks.guard]
alias = "g"
run = "bundle exec guard &"

[tasks.dev]
depends = ["guard"]
alias = "s"
dir = 'docs'
run = "sleep 2 && ./bin/dev"
